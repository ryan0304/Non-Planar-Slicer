# regression_ref/ manifest

Reference G-code files checked by `tools/check_regression.py`. If a reference
is ever regenerated on purpose (an intentional output change, not a bug),
update both the file and this manifest in the same commit, and explain why.

Regenerated 2026-07-18: the previous references predated the multi-printer
profile system (their header comment read `; Trident continuous non-planar
G-code`, i.e. `PrinterProfile.name == "Trident"`; the current default is
`"Voron Trident"`) and used a `SET_PRESSURE_ADVANCE` value sourced from an
OrcaSlicer install that's no longer the current default -- they were stale
snapshots from an earlier, undocumented version of the tool, not reproducible
invocations. None used `--filament`, deliberately: OrcaSlicer profile import
reads from `%APPDATA%`, which the regression suite must not depend on.

| File | Invocation |
|---|---|
| `ref_circle.gcode` | `python generate.py --out ref_circle.gcode` (all defaults) |
| `ref_star.gcode` | `python generate.py --shape star --out ref_star.gcode` |
| `ref_base_brim.gcode` | `python generate.py --base-layers 2 --brim 2 --out ref_base_brim.gcode` |
| `ref_textured.gcode` | `python generate.py --pattern ripple --pattern-amp 1.0 --pattern-waves 8 --out ref_textured.gcode` |
| `ref_profile_spiral.gcode` | Direct Python call (no CLI flag routes to `build_profile_spiral` -- it's only reachable via `serve.py`'s mesh-texture mode today). See `run_profile_spiral_case()` in `tools/check_regression.py` for the exact construction: `PrinterProfile()` defaults, `stack_from_shape(circle(30.0), 30.0, 60.0, 0.3, 240)`, `build_profile_spiral(..., z_amp=0.8, z_waves=4, base_layers=2)`. |
| `ref_loop_fabric.gcode` | Direct Python call (no CLI flag routes to `build_loop_fabric`). See `run_loop_fabric_case()` in `tools/check_regression.py`: `PrinterProfile()` defaults, `LoopSpec(loops_per_turn=24, row_mm=0.5, up_mm=0.8, stitch_mode="dip")`, `build_loop_fabric(..., shape=star(30.0, 5, 0.3), height=30.0, xy_twist_turns=0.0)`. Generated from PRE-change code (before the 2026-08-25 `xy_twist_turns` addition to `build_loop_fabric`), specifically to prove the twist formula (`theta - xy_twist_turns * 2.0 * math.pi * t`) is a bit-exact no-op at `xy_twist_turns=0.0` -- the post-change code was verified to reproduce this file byte-for-byte before `ref_loop_fabric_twist.gcode` below was generated. |
| `ref_loop_fabric_twist.gcode` | Same construction as `ref_loop_fabric.gcode` but `xy_twist_turns=1.5`. Generated from POST-change code; locks the twist formula and its sign (lobes advance counter-clockwise with height for positive twist, matching `spiral_path()` in `trident_gcode/paths.py`). |
| `ref_zone_overrides.gcode` | Added 2026-08-27 for the Zone Overrides feature (`SpiralSpec.zones`, `trident_gcode/paths.py`). No CLI flag routes to it. See `run_zone_override_case()` in `tools/check_regression.py`: `PrinterProfile()` defaults, `SpiralSpec(base_radius=30.0, height=30.0, layer_height=0.3, points_per_turn=240, xy_twist_turns=0.0, r_pattern="vwave", r_amp=1.0, zones=[ZoneOverride(t_lo=0.35, t_hi=0.70, blend=0.02, r_pattern="diamond", r_amp=2.0, xy_twist_turns=1.0)])`. The same function also asserts, in memory, that `zones=None` and `zones=[]` produce byte-identical `.text()` output -- proof the feature is a bit-exact no-op when unused, without a second reference file. |
| `ref_zone_overlap.gcode` | Added 2026-08-27 for Zone Overrides v2 (overlapping zones + per-zone `pattern_twist`/`r_twist_turns`, `spiral_path()`'s weighted-normalization crossfade). No CLI flag routes to it. See `run_zone_overlap_case()` in `tools/check_regression.py`: same `PrinterProfile()`/writer defaults as `ref_zone_overrides.gcode`, but with TWO overlapping zones sharing the band `0.45-0.60`: `ZoneOverride(t_lo=0.25, t_hi=0.60, blend=0.05, r_pattern="diamond", r_amp=3.0)` and `ZoneOverride(t_lo=0.45, t_hi=0.80, blend=0.05, r_pattern="pleats", r_amp=3.0, r_twist_turns=1.5)`. Locks the `max(1, sum-of-weights)` normalization formula and the per-zone texture-twist offset in the overlap region. `ref_zone_overrides.gcode` above (non-overlapping) is the proof this normalization is a byte-exact no-op when zones don't overlap -- it was NOT regenerated when overlap support was added. |
| `ref_profile_spiral_resume.gcode` | Added 2026-09-01 for `build_profile_spiral`'s new `resume: bool = False` parameter (`trident_gcode/generators/profile_spiral.py`), added for the hybrid planar+non-planar print feature so the non-planar wall can continue an already-started print (e.g. after an Orca-sliced planar base) without a second `PRINT_START`/safe-lift sequence. `resume=False` (the default) is proven byte-identical to today's output by `ref_profile_spiral.gcode` above, which was NOT regenerated. This case (`run_profile_spiral_resume_case()`) uses the same contour stack/`z_amp` as that one but calls `writer.header()` manually, moves the toolhead to an arbitrary position first (simulating a just-finished planar base), then calls `build_profile_spiral(..., base_z=10.0, base_layers=0, resume=True)`. Locks that `resume=True` skips `header()`/`safe_lift()` and picks up correctly from wherever the toolhead already is. |
| `ref_orca_replay.gcode` | Added 2026-09-01 for the hybrid planar+non-planar print feature's replay layer (`trident_gcode/orca_replay.py::replay_moves_onto_writer`, which turns parsed OrcaSlicer moves into `GcodeWriter` calls without ever trusting Orca's raw E/F values -- see `trident_gcode/orca_gcode_parser.py` for the parsing half). No CLI flag routes to it. See `run_orca_replay_case()` in `tools/check_regression.py`: same `PrinterProfile()`/writer defaults as the other direct-construction cases, fed a hand-built `list[OrcaMove]` (travel, 4-edge perimeter, retract, travel, unretract, 3-edge zig-zag infill, retract) -- not parsed from text, since that parsing step has its own suite (`tools/test_orca_gcode_parser.py`). Locks the derived `line_width_override` math (Orca's own `E-delta` + 3D segment length reproduces the same extruded volume through `extrusion_for_segment()`, byte-for-byte, with zero clamp events) and the retract/unretract/travel/extrude move classification. |
| `ref_hybrid_stitch.gcode` | Added 2026-09-01 for `trident_gcode/hybrid.py::build_hybrid_print`'s full stitch pipeline, WITHOUT touching `orca_slice.py`'s subprocess code: `tools/fixtures/orca_gcode/sample_base.gcode` (a small, realistic, checked-in sample -- also used by `tools/test_orca_gcode_parser.py`) stands in for "whatever OrcaSlicer would have produced," so this case never needs a live Orca install and can't be broken by Orca version drift. See `run_hybrid_stitch_case()` in `tools/check_regression.py`: `parse_orca_gcode()` the fixture -> `replay_moves_onto_writer()` -> a conditional `writer.retract()` (only if the replay didn't already end retracted, per `replay_report["ends_retracted"]` -- the fixture's own last move IS a retract, so this specific case exercises the "skip it" branch) -> `build_profile_spiral(resume=True, base_z=<fixture's own top Z>, base_layers=0, center=(102.5, 102.5), ...)`, the exact sequence `build_hybrid_print()` runs after slicing (its steps 8-13). Locks the whole handoff: header emitted exactly once, the wall resumes from wherever the replay left the toolhead, and no double-retract at the seam. |
| `ref_hybrid_print.gcode` | Added 2026-09-01. The only reference produced by an actual call to `trident_gcode/hybrid.py::build_hybrid_print` -- `ref_hybrid_stitch.gcode` above hand-inlines the post-slice sequence and so never enters `hybrid.py` at all, meaning it would keep passing even if `build_hybrid_print`'s own orchestration (header -> replay -> placement check -> conditional retract -> seam marker -> wall, its steps 8-12) were broken. This case exists to freeze that orchestration byte-for-byte across refactors of the function. See `run_hybrid_print_case()` in `tools/check_regression.py`: same `PrinterProfile()`/writer defaults as the other direct-construction cases (`line_width=0.45, layer_height=0.3, bed_temp=60.0, nozzle_temp=210.0, material="PLA", print_speed=40.0, first_layer_speed=20.0`), then `build_hybrid_print(writer, shape_fn=circle(3.0), radius=3.0, height=20.0, transition_height=0.4, layer_height=0.3, points_per_turn=60, wall_count=2, infill_density=0.2, infill_pattern="grid", orca_path="unused-because-monkeypatched", center=(102.5, 102.5), z_amp=0.3, z_waves=3)` -- the same arguments as `tools/test_hybrid.py`'s happy path. The ONLY thing stubbed is the slice subprocess: the module-level name `trident_gcode.hybrid.slice_stl_to_gcode` is monkeypatched (and restored in a `finally`) to a `_fake_slice` returning the text of `tools/fixtures/orca_gcode/sample_base.gcode`, exactly as `tools/test_hybrid.py:42-51` does, so this case needs no live OrcaSlicer install and can't be broken by Orca version drift. Everything else is the real code path (STL build, machine/process/filament JSON, parse, replay, placement check, retract decision, seam marker, upper contour stack, wall). `center=(102.5, 102.5)` is load-bearing, not arbitrary: it must match the fixture's own footprint or step 10's placement sanity check raises `ValueError`. To regenerate deliberately: run `run_hybrid_print_case()` with `REF_DIR` pointed at a scratch directory containing an empty `ref_hybrid_print.gcode` placeholder (the case writes its output to `tmpdir` before comparing) and copy the produced file over this one. Verified deterministic: two consecutive runs are byte-identical. Regenerated 2026-09-03: `fan_off_layers` (unset/0 in this case's own arguments) is now counted from the absolute start of the print (base + wall combined) instead of restarting at the wall's own layer 0 -- the Orca-sliced base above never issues a fan command of its own (`orca_replay.py` never reads fan state from Orca's text), so under the old behavior the fan waited a full extra wall turn past the seam before turning on. Only diff: the single `M106 S255` line moved from immediately after the wall's first full turn to immediately after the `; hybrid: non-planar wall begins here` seam comment -- no coordinate, extrusion, or timing value on any other line changed (verified with a direct `diff` against the previous reference before regenerating). Regenerated 2026-09-07: the wall's start point used to always sit at `shape_fn`'s fixed `theta=0`, unrelated to wherever Orca's own seam algorithm actually finished the base -- so the "move to profile spiral start" travel right after `resume=True` could cross most of the part's diameter for no reason. `build_hybrid_print` now probes the wall's own t=0 ring (`stack_from_shape`, one extra cheap single-layer call) at `theta0=0`, finds the point nearest wherever the replay actually left the toolhead (`_nearest_ring_index`), and re-anchors the whole upper contour stack's angle grid there via `stack_from_shape`'s new `theta0` parameter (`trident_gcode/profile_stack.py`) -- a uniform per-layer rotation, so every per-index effect (cage/texture/twist) is unaffected, only which physical point is labelled index 0. Verified against the real fixture: the travel from the base's own last point to the wall's first point shrank from roughly 6.0 mm to about 0.56 mm. Also carries the new `z_hop_type` parameter (`"auto"` default, byte-identical -- this case does not set it) added the same day for the seam travel's shape (Normal/Slope/Spiral alternatives to the diagonal-then-vertical-drop default). |
| `ref_mesh_hybrid_print.gcode` | Added 2026-09-01, previously undocumented here (a pre-existing gap in this manifest, not introduced by this entry). The mesh sibling of `ref_hybrid_print.gcode` above: the planar base is `tools/fixtures/meshes/hex_mount.stl` (a small hexagonal prism) handed to Orca as-is, sliced with the same monkeypatched `_fake_slice` returning `tools/fixtures/orca_gcode/sample_base.gcode`, and the non-planar wall continues from the mesh's real top contour. See `run_mesh_hybrid_print_case()` in `tools/check_regression.py`: `layer_height=0.2`, `scale=0.1` (shrinks the checked-in 4.5 mm mesh so its layer-snapped base height matches the Orca fixture's own extruding-move top, or the Orca-Z-drift check raises), `center=(102.5, 102.5)` (same placement-check constraint as `ref_hybrid_print.gcode`), `shape_fn=circle(2.5), radius=2.5, height=3.0, blend_height=1.0, wall_count=2, infill_density=0.2, infill_pattern="grid", z_amp=0.3, z_waves=3`. Regenerated 2026-09-03 for the identical fan-timing fix described in `ref_hybrid_print.gcode`'s own entry above (`build_mesh_hybrid_print` shares the same `fan_off_layers`-now-counts-the-base logic) -- same single-line diff, the `M106 S255` line moved earlier by one wall turn to land right after the seam comment, nothing else changed. Regenerated 2026-09-07 for the identical seam-alignment fix described in `ref_hybrid_print.gcode`'s own entry above. Here the rotation is computed once (`_nearest_ring_index` against `mesh_ring`, the already-sampled seam ring from `top_contour_from_mesh`) and applied TWICE consistently -- the `mesh_ring` array itself is rolled by the found index (exact, no second ray-cast needed), and the same index (as `theta0`) is forwarded into `blend_stack`'s own internal `stack_from_shape` call for the parametric rings above it -- so ring 0 (the mesh ring, assigned exactly by `blend_stack`) and every layer above it stay index-aligned under one consistent rotation, preserving the seam-blend interpolation's point-by-point correspondence. |
| `ref_loop_hybrid_print.gcode` | Added 2026-09-09 for `trident_gcode/hybrid.py::build_loop_hybrid_print` -- Phase 1 of "real Orca-sliced planar base + Loop Fabric wall" support (previously the two were mutually exclusive; a hybrid base request was silently dropped whenever Loop Fabric was also active). Exercises the new `resume`/`base_z`/`seam_theta`/`fan_already_on` kwargs on `build_loop_fabric` (`trident_gcode/generators/loop_fabric.py`): the solid cuff and `header()` are skipped entirely, row 0 anchors to the Orca-sliced base's own top instead of a cuff top, and stitch angles are re-anchored to the seam-probe angle found via `_nearest_ring_index` (the same helper `build_hybrid_print` already uses for its own `theta0`). See `run_loop_hybrid_print_case()` in `tools/check_regression.py`: same base/seam geometry as `ref_hybrid_print.gcode` (`shape_fn=circle(3.0), radius=3.0, height=20.0, transition_height=0.4, layer_height=0.3, points_per_turn=60, wall_count=2, infill_density=0.2, infill_pattern="grid", center=(102.5, 102.5)`, same monkeypatched `_fake_slice` returning `tools/fixtures/orca_gcode/sample_base.gcode`) but with `loop_spec=LoopSpec(loops_per_turn=24, row_mm=0.5, up_mm=0.8, stitch_mode="dip")` (matching `ref_loop_fabric.gcode`'s own non-hybrid spec) instead of `z_amp`/`z_waves`. `center=(102.5, 102.5)` is load-bearing, not arbitrary, for the same placement-check reason as `ref_hybrid_print.gcode`. Verified deterministic: two consecutive runs are byte-identical. Every pre-existing reference (including `ref_loop_fabric.gcode`/`ref_loop_fabric_twist.gcode` and every `ref_hybrid_*`/`ref_mesh_hybrid_*` file) was re-checked byte-identical after adding the new `resume`/`base_z`/`seam_theta`/`fan_already_on` parameters -- all four default to values that are a no-op for every existing caller. |
| `ref_mesh_loop_hybrid_print.gcode` | Added 2026-09-09 for `trident_gcode/hybrid.py::build_mesh_loop_hybrid_print` -- Phase 2 of "real Orca-sliced planar base + Loop Fabric wall" support: the mesh sibling of `ref_loop_hybrid_print.gcode` above, using the SAME `tools/fixtures/meshes/hex_mount.stl` planar base as `ref_mesh_hybrid_print.gcode` but with a Loop Fabric wall resumed and BLENDED on top of it instead of the parametric `blend_stack`+`build_profile_spiral` wall. Exercises the new `blend_ring`/`blend_height`/`seam_style`/`seam_coverage` kwargs on `build_loop_fabric` (`trident_gcode/generators/loop_fabric.py`) together with Phase 1's `resume`/`base_z`/`seam_theta`/`fan_already_on`: the wall's shape function blends continuously from the mesh's own seam outline (`trident_gcode/profile_stack.py::mesh_ring_radius_at`, angular interpolation -- NOT the discrete index-aligned `blend_stack` scheme) toward the parametric circle over `blend_height`, and the seam-probe angle is found via `_nearest_ring_index` against the UNROLLED `mesh_ring` (deliberately not rolled the way `build_mesh_hybrid_print` rolls its own copy -- see `build_mesh_loop_hybrid_print`'s own docstring for why the continuous blend needs the ring to stay in its native, unrotated angle frame). See `run_mesh_loop_hybrid_print_case()` in `tools/check_regression.py`: same mesh/base geometry as `ref_mesh_hybrid_print.gcode` (`layer_height=0.2, scale=0.1, center=(102.5, 102.5), shape_fn=circle(2.5), radius=2.5, height=3.0, blend_height=1.0, wall_count=2, infill_density=0.2, infill_pattern="grid"`, same monkeypatched `_fake_slice` returning `tools/fixtures/orca_gcode/sample_base.gcode`) but with `loop_spec=LoopSpec(loops_per_turn=24, row_mm=0.5, up_mm=0.8, stitch_mode="dip")` (matching `ref_loop_hybrid_print.gcode`'s own spec) instead of `z_amp`/`z_waves`, and default `seam_style="fillet"`/`seam_coverage=1.0`. `center=(102.5, 102.5)` is load-bearing, not arbitrary, for the same placement-check reason as `ref_mesh_hybrid_print.gcode`. Verified deterministic: two consecutive runs are byte-identical. Every pre-existing reference (all 15 before this one, including every `ref_hybrid_*`/`ref_mesh_hybrid_*`/`ref_loop_*` file) was re-checked byte-identical after adding the new `blend_ring`/`blend_height`/`seam_style`/`seam_coverage` parameters to `build_loop_fabric` -- all four default to values that are a no-op for every existing caller (`blend_ring=None` skips the whole blend code path). |
| `ref_surface_spiral.gcode` | Added 2026-09-12. The first byte-exact case for `build_surface_spiral` (`trident_gcode/generators/surface_spiral.py`) -- the non-planar conformal shell had **no** regression coverage at all until now, despite being the least mature generator in the app. No CLI case routes to it (`generate.py --surface` was never in `CLI_CASES`), so it is built by direct Python call like `ref_profile_spiral.gcode`. See `run_surface_spiral_case()` in `tools/check_regression.py`: same `PrinterProfile()`/writer defaults as the other direct-construction cases (`line_width=0.45, layer_height=0.3, bed_temp=60.0, nozzle_temp=210.0, material="PLA", print_speed=40.0, first_layer_speed=20.0`), then `build_surface_spiral(writer, dome(12.0, 4.0), 12.0, shells=2)` -- a 12 mm dome of 4 mm height, two stacked conformal shells, and **no adhesion arguments at all**. Generated from PRE-change code (before the same day's bed-adhesion package was added to that module), specifically to prove every new parameter -- `first_layer_squish`, `first_layer_spacing_factor`, `first_layer_flow`, `base_layers`, `brim_loops`, `base_style`, `base_points_per_turn` -- is a bit-exact no-op at its default, exactly as `ref_loop_fabric.gcode` was generated pre-change for `xy_twist_turns`. The post-change code was verified to reproduce this file byte-for-byte before `ref_surface_spiral_adhesion.gcode` below was generated. Verified deterministic: two consecutive runs of the pre-change code are byte-identical. |
| `ref_surface_spiral_adhesion.gcode` | Added 2026-09-12 for the surface/conformal mode's new bed-adhesion package. Until this change the mode offered no adhesion whatsoever -- the shell began as a thin spiral laid straight onto bare glass at `first_layer_z`, the only print mode in the app without a first-layer squish or an optional base disk and brim. The geometry is reused from `trident_gcode/generators/base_fill.py` (the shell's footprint is a plain disk of `radius`, which trivially satisfies that module's star-convexity requirement) rather than written fresh. See `run_surface_spiral_adhesion_case()` in `tools/check_regression.py`: **identical** field/radius/shells/writer to `ref_surface_spiral.gcode` above, plus `first_layer_squish=0.75, first_layer_spacing_factor=1.25, base_layers=2, brim_loops=3` -- so the only difference between the two references is the adhesion package itself, making the pair a direct before/after of the feature rather than two unrelated prints. Locks in particular the geometric invariant the whole change turns on: the first disk is squished to `0.75 * 0.3 = 0.225` mm, the second sits at `0.525`, and the shell's floor lands at `first_layer_z + base_layers * layer_height = 0.825` -- exactly one layer above the top disk, so the conformal shell neither re-prints into its own base nor floats above it. Also locks the seam handling (the shell starts at the CENTRE while the base/brim ends on the OUTLINE, so the one unavoidable travel in this mode retracts and lifts clear of the finished base instead of dragging across it) and the brim-aware bed-fit radius (`12 + 3 * 0.75 = 14.25` mm). Verified deterministic: two consecutive runs are byte-identical. Every pre-existing reference (all 16 before these two) was re-checked byte-identical after the change. |

`test_circle.gcode` (byte-identical duplicate of `ref_circle.gcode`) and
`test_profile_spiral.gcode` (superseded by `ref_profile_spiral.gcode`) were
removed as part of this regeneration.
